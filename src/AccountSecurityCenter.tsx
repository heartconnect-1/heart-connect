import {useEffect,useState} from 'react';import {api} from '@appdeploy/client';import {ShieldCheck,X,LockKeyhole,Mail,Phone,MonitorSmartphone,LogOut,KeyRound,AlertTriangle,CheckCircle2} from 'lucide-react';import './auth.css';type Session={device:string;browser:string;approximateLocation:string;lastActive:number;current:boolean;expires:string};type Security={role:string;email?:string;provider:{supabaseConfigured:boolean;emailPasswordReady:boolean;phoneOtpReady:boolean};sessions:Session[];mfaReady:boolean;alerts:Array<{text:string;time:number}>};export default function AccountSecurityCenter({open,onClose,onSignOut}:{open:boolean;onClose:()=>void;onSignOut:()=>Promise<void>}){const[data,setData]=useState<Security|null>(null),[notice,setNotice]=useState('');async function load(){try{const r=await api.get('/api/auth/security');setData(r.data)}catch{setNotice('Could not load account security information.')}}useEffect(()=>{if(open)load()},[open]);if(!open)return null;async function providerAction(path:string){try{await api.post(path,{});setNotice('Security request completed.');await load()}catch{setNotice('This control requires the dedicated Supabase identity provider before it can safely change credentials or revoke other-device sessions. No security action was falsely reported as completed.')}}return <div className='security-overlay'><section className='security-center'><button className='security-close' onClick={onClose}><X/></button><header><span><ShieldCheck/></span><div><small>HEART CONNECT SECURITY CENTER</small><h2>Protect your account</h2><p>Private account controls stay separate from your public dating profile.</p></div></header>{notice&&<div className='auth-notice'><AlertTriangle/>{notice}</div>}<div className='security-grid'><article><h3><LockKeyhole/> Account</h3><div className='security-row'><span>Role</span><b>{data?.role||'User'}</b></div><div className='security-row'><span>Email</span><b>{data?.email||'Provider managed'}</b></div><button onClick={()=>providerAction('/api/auth/security/change-password')}><KeyRound/> Change password</button><button onClick={()=>providerAction('/api/auth/security/update-email')}><Mail/> Update / verify email</button><button onClick={()=>providerAction('/api/auth/security/update-phone')}><Phone/> Update / verify phone</button></article><article><h3><MonitorSmartphone/> Active sessions</h3>{data?.sessions?.map((s,i)=><div className='session-card' key={i}><div><b>{s.device} · {s.browser}</b><span>{s.approximateLocation}</span><small>{s.current?'Current session · ':''}Last active {new Date(s.lastActive).toLocaleString()}</small><small>{s.expires}</small></div>{s.current&&<CheckCircle2/>}</div>)}<button className='danger-outline' onClick={()=>providerAction('/api/auth/security/logout-all')}><LogOut/> Logout all devices</button></article><article><h3><ShieldCheck/> Two-factor authentication</h3><p>{data?.mfaReady?'MFA provider is ready for enrollment.':'MFA architecture is reserved, but enrollment remains disabled until Supabase is connected.'}</p><button onClick={()=>providerAction('/api/auth/security/mfa/start')} disabled={!data?.mfaReady}>Set up two-factor authentication</button><div className='security-status'><span className={data?.provider?.supabaseConfigured?'good':''}>{data?.provider?.supabaseConfigured?'Supabase credentials detected':'Supabase not connected yet'}</span><span className={data?.provider?.phoneOtpReady?'good':''}>{data?.provider?.phoneOtpReady?'Phone OTP ready':'Phone OTP pending'}</span></div></article><article><h3><AlertTriangle/> Suspicious login alerts</h3>{data?.alerts?.length?data.alerts.map((a,i)=><div className='alert-row' key={i}><AlertTriangle/><span>{a.text}<small>{new Date(a.time).toLocaleString()}</small></span></div>):<p>No stored suspicious-login alerts are available in the current provider. Future alerts will appear here rather than as public profile data.</p>}</article></div><footer><button className='danger-solid' onClick={async()=>{if(!confirm('Log out of this device?'))return;await onSignOut();onClose()}}><LogOut/> Log out of this device</button><button className='auth-secondary' onClick={onClose}>Close</button></footer></section></div>}
