(()=>{
if(window.__hcTravelInteractionsV1)return;window.__hcTravelInteractionsV1=true;
const style=document.createElement('style');style.textContent='.hc-card-link{cursor:pointer;position:relative}.hc-card-link:focus-visible{outline:3px solid rgba(236,72,117,.35);outline-offset:3px}.hc-card-link::after{content:"→";position:absolute;right:16px;bottom:14px;font-weight:900;color:#ec4875;opacity:.75}.region.hc-card-link::after{right:12px;bottom:10px}.trip-list .hc-card-link::after{right:12px;bottom:10px;font-size:12px}.hc-card-link:hover::after{transform:translateX(3px);opacity:1}.hc-card-link::after{transition:.18s ease}';document.head.appendChild(style);
const enquiry=()=>document.getElementById('enquiry');
function go(href,prefill=''){
 if(href==='#enquiry'){
   if(prefill){const d=document.getElementById('destination');if(d&&!d.value)d.value=prefill}
   enquiry()?.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'','#enquiry');return;
 }
 location.href=href;
}
function bind(el,href,label,prefill=''){
 if(!el||el.dataset.hcClickable==='1')return;el.dataset.hcClickable='1';el.classList.add('hc-card-link');el.tabIndex=0;el.setAttribute('role','link');el.setAttribute('aria-label',label);
 const activate=e=>{if(e.type==='click'&&e.target.closest?.('a,button,input,select,textarea,summary'))return;if(e.type==='keydown'&&!['Enter',' '].includes(e.key))return;e.preventDefault();go(href,prefill)};
 el.addEventListener('click',activate);el.addEventListener('keydown',activate);
}
const services=[
 ['#enquiry','Start a holiday planning enquiry'],
 ['/bookings/?type=bnb','Open BnB accommodation booking'],
 ['#enquiry','Ask about airport and local transfers'],
 ['/bookings/?type=date','Open date and experience booking'],
 ['#enquiry','Request a custom travel itinerary'],
 ['/safety/','Open Heart Connect travel and dating safety guidance']
];
document.querySelectorAll('.service-grid .service-card').forEach((el,i)=>{const x=services[i];if(x)bind(el,x[0],x[1])});
const quick=[
 ['/bookings/?type=bnb','Open accommodation booking'],
 ['#enquiry','Ask about flights and transfers'],
 ['/bookings/?type=date','Open dates and experiences booking'],
 ['#enquiry','Request a custom itinerary']
];
document.querySelectorAll('.trip-list>div').forEach((el,i)=>{const x=quick[i];if(x)bind(el,x[0],x[1])});
document.querySelectorAll('.region-grid .region').forEach(el=>{const name=el.querySelector('b')?.textContent?.trim()||'';bind(el,'#enquiry','Plan travel in '+name,name)});
})();