import upstreamWorker,{isNavigation} from './worker.js';

const REGION_DATA='https://cdn.jsdelivr.net/npm/country-region-data@4.1.0/data.json';
const EDGE_VERSION='cloudflare-router-v6';

const BOOKING_DASHBOARD_JS=`(()=>{let scheduled=false;function style(){if(document.getElementById('hc-booking-dashboard-style'))return;const s=document.createElement('style');s.id='hc-booking-dashboard-style';s.textContent='.hc-client-booking-dashboard{margin:18px 0 22px;padding:18px;border:1px solid rgba(236,72,117,.2);border-radius:20px;background:linear-gradient(135deg,rgba(236,72,117,.08),rgba(93,63,211,.06));box-shadow:0 10px 30px rgba(15,23,42,.06)}.hc-client-booking-dashboard h2{margin:0 0 4px;font-size:22px}.hc-client-booking-dashboard>p{margin:0;color:#76636b;font-size:12px}.hc-booking-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px;margin:14px 0}.hc-booking-stat{padding:12px;border-radius:14px;background:rgba(255,255,255,.72);border:1px solid rgba(120,90,105,.14)}.hc-booking-stat b,.hc-booking-stat span{display:block}.hc-booking-stat b{font-size:22px}.hc-booking-stat span{font-size:10px;color:#76636b}.hc-booking-actions{display:flex;gap:8px;flex-wrap:wrap}.hc-booking-actions a{min-height:40px;padding:9px 13px;border-radius:11px;text-decoration:none;font-weight:800;display:inline-flex;align-items:center;background:#ed174f;color:#fff}.hc-booking-actions a.secondary{background:transparent;color:inherit;border:1px solid rgba(120,90,105,.24)}.hc-booking-note{margin-top:11px!important;font-size:10px!important}.hc-bookings-shortcut .hc-booking-emoji{font-size:24px;color:var(--color-primary,#ed174f)!important}.hc-date-venue-note{display:block;margin-top:5px;color:#76636b;font-size:11px;line-height:1.35}@media(max-width:680px){.hc-booking-stats{grid-template-columns:repeat(2,1fr)}.hc-client-booking-dashboard{padding:14px}.hc-booking-actions a{flex:1;justify-content:center}}';document.head.appendChild(s)}function text(el){return String(el?.textContent||'').trim().toLowerCase()}function enhanceBooking(){const grid=document.querySelector('.booking-grid'),main=grid?.parentElement;if(!grid||!main)return;const articles=[...grid.querySelectorAll('aside article')],statuses=articles.map(a=>text(a.querySelector('.status'))),total=articles.length,requested=statuses.filter(x=>x==='requested').length,approved=statuses.filter(x=>x==='approved').length,confirmed=statuses.filter(x=>x==='confirmed').length;let box=document.querySelector('.hc-client-booking-dashboard');if(!box){box=document.createElement('section');box.className='hc-client-booking-dashboard';main.insertBefore(box,grid)}const sig=[total,requested,approved,confirmed].join('|');if(box.dataset.sig!==sig){box.dataset.sig=sig;box.innerHTML='<h2>My Booking Dashboard</h2><p>Manage BnB stays, date plans, documents and payments from one place.</p><div class="hc-booking-stats"><div class="hc-booking-stat"><b>'+total+'</b><span>Total requests</span></div><div class="hc-booking-stat"><b>'+requested+'</b><span>Awaiting approval</span></div><div class="hc-booking-stat"><b>'+approved+'</b><span>Ready for payment</span></div><div class="hc-booking-stat"><b>'+confirmed+'</b><span>Confirmed</span></div></div><div class="hc-booking-actions"><a href="?type=bnb">Book a BnB</a><a href="?type=date">Plan a Date</a><a class="secondary" href="../app/">Heart Connect account</a></div><p class="hc-booking-note">Payment is only treated as successful after trusted M-Pesa or Paystack verification. Requested bookings are not confirmed reservations.</p>'}const areaNote=document.querySelector('.hc-area-note');if(areaNote)areaNote.textContent='Choose or type an area, estate, neighborhood or nearby place. Suggestions use geographic place data.';const venue=[...document.querySelectorAll('label')].find(l=>text(l).startsWith('venue preference')),input=venue?.querySelector('input');if(input){let dl=document.getElementById('hc-date-venue-options');if(!dl){dl=document.createElement('datalist');dl.id='hc-date-venue-options';['Restaurant','Café','Rooftop','Hotel lounge','Fine dining','Casual dining','Coffee date','Picnic','Cinema','Museum or gallery','Live music','Beach','Park or garden','Activity or experience'].forEach(v=>dl.appendChild(new Option(v)));document.body.appendChild(dl)}input.setAttribute('list','hc-date-venue-options');if(!venue.querySelector('.hc-date-venue-note')){const n=document.createElement('small');n.className='hc-date-venue-note';n.textContent='Choose a venue style or type your own. Heart Connect will coordinate the final venue after availability review.';venue.appendChild(n)}}}function enhanceApp(){const dash=document.querySelector('.s5-dashboard');if(!dash||dash.querySelector('.hc-bookings-shortcut'))return;const b=document.createElement('button');b.type='button';b.className='hc-bookings-shortcut';b.setAttribute('aria-label','Open my bookings and trips');b.innerHTML='<span class="hc-booking-emoji">▣</span><b>Bookings & Trips</b><span>BnBs, date plans, documents and payments</span>';b.addEventListener('click',()=>location.assign('../bookings/'));dash.appendChild(b)}function enhance(){style();if(location.pathname==='/bookings'||location.pathname.startsWith('/bookings/'))enhanceBooking();if(location.pathname==='/app'||location.pathname.startsWith('/app/'))enhanceApp()}new MutationObserver(()=>{if(scheduled)return;scheduled=true;queueMicrotask(()=>{scheduled=false;enhance()})}).observe(document.documentElement,{childList:true,subtree:true});addEventListener('DOMContentLoaded',enhance);enhance()})();`;

function edgeResponse(response){const headers=new Headers(response.headers);headers.set('x-heart-connect-edge',EDGE_VERSION);return new Response(response.body,{status:response.status,statusText:response.statusText,headers})}
function jsonItems(items,status=200){return new Response(JSON.stringify({items}),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'public, max-age=86400','x-content-type-options':'nosniff','x-heart-connect-edge':EDGE_VERSION}})}
function clean(v,max=200){return String(v??'').replace(/[\u0000-\u001f\u007f]/g,' ').replace(/\s+/g,' ').trim().slice(0,max)}
function emailOk(v){return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(v||''))}
async function regionsV6(country,iso2){const iso=clean(iso2,3).toUpperCase();if(!iso||iso==='KE')return null;try{const r=await fetch(REGION_DATA,{headers:{accept:'application/json'},cf:{cacheTtl:86400,cacheEverything:true}});if(!r.ok)return null;const data=await r.json(),row=(Array.isArray(data)?data:[]).find(x=>String(x?.countryShortCode||'').toUpperCase()===iso)||data.find?.(x=>String(x?.countryName||'').toLowerCase()===String(country||'').toLowerCase());const regions=Array.isArray(row?.regions)?row.regions:[];if(!regions.length)return null;const items=regions.map(x=>{const name=clean(x?.name,120),short=clean(x?.shortCode,20);return name?{name,code:short?iso+'-'+short:undefined}:null}).filter(Boolean).sort((a,b)=>a.name.localeCompare(b.name));return items.length?jsonItems(items):null}catch{return null}}
async function sendResend(env,to,subject,text,replyTo,keySuffix){const key=String(env.RESEND_API_KEY||'').trim();if(!key||!emailOk(to))return;const body={from:'Heart Connect Bookings <noreply@royal-heart.com>',to:[to],subject,text};if(emailOk(replyTo))body.reply_to=replyTo;await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:'Bearer '+key,'Content-Type':'application/json','Idempotency-Key':'hc-booking-'+keySuffix},body:JSON.stringify(body)}).catch(()=>null)}
async function sendBookingAlerts(env,payload,result){const id=clean(result?.id,120);if(!id||!env.RESEND_API_KEY)return;const kind=clean(payload?.kind,20)==='bnb'?'BnB stay':'Date plan',guestName=clean(payload?.guestName,100)||'Heart Connect member',guestEmail=clean(payload?.guestEmail,254).toLowerCase(),guestPhone=clean(payload?.guestPhone,60),location=[clean(payload?.area,100),clean(payload?.locality||payload?.city||payload?.destination,100),clean(payload?.region,100),clean(payload?.country,80)].filter(Boolean).join(', ')||'Not provided',when=kind==='BnB stay'?[clean(payload?.checkIn,20),clean(payload?.checkOut,20)].filter(Boolean).join(' → '):[clean(payload?.preferredDate,20),clean(payload?.preferredTime,20)].filter(Boolean).join(' at '),amount=Number(result?.amountKes||0),admin=clean(env.BOOKING_ADMIN_EMAIL||'admin@royal-heart.com',254);const adminText=['New Heart Connect booking request','',kind+' reference: '+id,'Customer: '+guestName,guestEmail?'Email: '+guestEmail:'',guestPhone?'Phone: '+guestPhone:'','Location: '+location,when?'Date/stay: '+when:'',amount?'Requested amount: KES '+amount.toLocaleString():'','Status: REQUESTED — not yet confirmed','','Review now: https://royal-heart.com/admin/#/bookings'].filter(Boolean).join('\n');await sendResend(env,admin,'[Heart Connect Bookings] New '+kind+' request '+id,adminText,guestEmail,id+'-admin');if(emailOk(guestEmail)){const customerText=['Hi '+guestName+',','','We received your Heart Connect '+kind.toLowerCase()+' request.','Reference: '+id,'Location: '+location,when?'Date/stay: '+when:'',amount?'Requested amount: KES '+amount.toLocaleString():'','','This is a booking request, not a confirmed reservation yet. Please wait for availability/venue approval before making payment.','You can manage your request at https://royal-heart.com/bookings/'].filter(Boolean).join('\n');await sendResend(env,guestEmail,'Heart Connect booking request received — '+id,customerText,'',id+'-customer')}}

export default {async fetch(request,env,ctx){const incoming=new URL(request.url);if(incoming.pathname==='/_hc/booking-dashboard.js')return new Response(BOOKING_DASHBOARD_JS,{headers:{'content-type':'application/javascript; charset=utf-8','cache-control':'public, max-age=300','x-content-type-options':'nosniff','x-heart-connect-edge':EDGE_VERSION}});if(incoming.pathname==='/_hc/regions'){const better=await regionsV6(String(incoming.searchParams.get('country')||''),String(incoming.searchParams.get('iso2')||''));if(better)return better}const alertable=request.method==='POST'&&(incoming.pathname==='/api/bookings'||incoming.pathname==='/api/bookings/guest');let payload=null;if(alertable){try{payload=await request.clone().json()}catch{}}let response=await upstreamWorker.fetch(request,env,ctx);if(alertable&&response.ok&&payload){try{const result=await response.clone().json(),task=sendBookingAlerts(env,payload,result);if(ctx?.waitUntil)ctx.waitUntil(task);else void task}catch{}}const navigation=isNavigation(request)&&!incoming.pathname.startsWith('/api/');if(navigation&&response.ok&&(response.headers.get('content-type')||'').includes('text/html')){const isBooking=incoming.pathname==='/bookings'||incoming.pathname.startsWith('/bookings/'),isApp=incoming.pathname==='/app'||incoming.pathname.startsWith('/app/');if(isBooking||isApp)response=new HTMLRewriter().on('head',{element(element){element.append('<script src="/_hc/booking-dashboard.js" defer></script>',{html:true})}}).transform(response)}return edgeResponse(response)}};
