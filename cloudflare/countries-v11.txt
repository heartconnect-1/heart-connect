(()=>{const OTHER='Other / not listed';const CODES='AD AE AF AG AI AL AM AO AQ AR AS AT AU AW AX AZ BA BB BD BE BF BG BH BI BJ BL BM BN BO BQ BR BS BT BV BW BY BZ CA CC CD CF CG CH CI CK CL CM CN CO CR CU CV CW CX CY CZ DE DJ DK DM DO DZ EC EE EG EH ER ES ET FI FJ FK FM FO FR GA GB GD GE GF GG GH GI GL GM GN GP GQ GR GS GT GU GW GY HK HM HN HR HT HU ID IE IL IM IN IO IQ IR IS IT JE JM JO JP KE KG KH KI KM KN KP KR KW KY KZ LA LB LC LI LK LR LS LT LU LV LY MA MC MD ME MF MG MH MK ML MM MN MO MP MQ MR MS MT MU MV MW MX MY MZ NA NC NE NF NG NI NL NO NP NR NU NZ OM PA PE PF PG PH PK PL PM PN PR PS PT PW PY QA RE RO RS RU RW SA SB SC SD SE SG SH SI SJ SK SL SM SN SO SR SS ST SV SX SY SZ TC TD TF TG TH TJ TK TL TM TN TO TR TT TV TW TZ UA UG UM US UY UZ VA VC VE VG VI VN VU WF WS YE YT ZA ZM ZW XK'.split(' ');const countryCode=new Map(),regionCode=new Map(),resultCache=new Map(),pending=new Map(),boundArea=new WeakSet(),boundCountry=new WeakSet(),boundRegion=new WeakSet(),boundCity=new WeakSet();let countryNames=[],scheduled=false,areaTimer=0,lastCountry='',lastRegion='',lastCity='';try{const dn=new Intl.DisplayNames(['en'],{type:'region'});countryNames=CODES.map(code=>{const name=dn.of(code);if(name)countryCode.set(name,code);return name}).filter(Boolean).filter((v,i,a)=>a.indexOf(v)===i).sort((a,b)=>a.localeCompare(b))}catch{return}function label(prefix){return[...document.querySelectorAll('label')].find(l=>(l.textContent||'').trim().startsWith(prefix))||null}function country(){return label('Country')?.querySelector('select')||null}function region(){return label('State / County / Province')?.querySelector('select')||null}function city(){return label('City / Town')?.querySelector('select')||null}function area(){return label('Area / Neighborhood')?.querySelector('input')||null}function clean(items){return[...new Set((Array.isArray(items)?items:[]).map(x=>String(typeof x==='string'?x:x?.name||'').trim()).filter(Boolean))].sort((a,b)=>a.localeCompare(b))}function addNote(parent,cls,text){if(!parent||parent.querySelector('.'+cls))return;const n=document.createElement('small');n.className=cls;n.textContent=text;n.style.cssText='display:block;margin-top:5px;color:#76636b;font-size:11px;line-height:1.35';parent.appendChild(n)}function ensureCountries(){const s=country();if(!s)return;if(!boundCountry.has(s)){boundCountry.add(s);const existing=new Set([...s.options].map(o=>o.value||o.text)),other=[...s.options].find(o=>(o.value||o.text)===OTHER);for(const name of countryNames){if(existing.has(name))continue;const o=new Option(name,name);other?s.insertBefore(o,other):s.add(o)}s.addEventListener('change',()=>{lastCountry='';lastRegion='';lastCity='';const r=region(),c=city();if(r){r.innerHTML='';r.add(new Option('Loading regions…',''));r.disabled=true}if(c){c.innerHTML='';c.add(new Option('Choose a region first',''));c.disabled=true}schedule(true)})}addNote(label('Country'),'hc-global-country-note','Country → matching region → matching city loads automatically. “Other / not listed” is only a fallback.')}async function load(url){if(resultCache.has(url))return resultCache.get(url);if(pending.has(url))return pending.get(url);const task=(async()=>{try{const r=await fetch(url,{credentials:'same-origin'});const b=await r.json();if(!r.ok||!Array.isArray(b.items))throw 0;resultCache.set(url,b.items);return b.items}catch{return null}finally{pending.delete(url)}})();pending.set(url,task);return task}function apply(s,items,placeholder,selected){if(!s)return;const names=clean(items),sig=['',...names,OTHER].join('\u0001'),old=[...s.options].map(o=>o.value).join('\u0001');if(sig!==old){s.innerHTML='';s.add(new Option(placeholder,''));for(const raw of items){const name=String(typeof raw==='string'?raw:raw?.name||'').trim();if(!name)continue;const o=new Option(name,name);if(typeof raw==='object'&&raw?.code)o.dataset.code=String(raw.code);s.add(o)}s.add(new Option(OTHER,OTHER))}if(selected&&[...s.options].some(o=>o.value===selected))s.value=selected;s.disabled=false}async function syncRegions(force=false){const c=country(),r=region();if(!c||!r)return;const name=c.value;if(!name||name===OTHER)return;if(!force&&lastCountry===name&&r.dataset.hcCountry===name)return;lastCountry=name;const iso=countryCode.get(name)||'',items=await load('/_hc/regions?country='+encodeURIComponent(name)+'&iso2='+encodeURIComponent(iso));if(!items||country()?.value!==name)return;const selected=r.value;apply(r,items,'Choose state / county / province',selected);r.dataset.hcCountry=name;regionCode.clear();for(const x of items)if(typeof x==='object'&&x?.name&&x?.code)regionCode.set(String(x.name),String(x.code));bindRegion(r);if(r.value&&r.value!==OTHER)void syncCities(true)}async function syncCities(force=false){const c=country(),r=region(),s=city();if(!c||!r||!s)return;const cn=c.value,rn=r.value;if(!cn||cn===OTHER||!rn||rn===OTHER)return;const key=cn+'|'+rn;if(!force&&lastRegion===key&&s.dataset.hcRegion===key)return;lastRegion=key;const iso=countryCode.get(cn)||'',stateCode=regionCode.get(rn)||r.selectedOptions?.[0]?.dataset?.code||'',items=await load('/_hc/cities?country='+encodeURIComponent(cn)+'&iso2='+encodeURIComponent(iso)+'&state='+encodeURIComponent(rn)+'&stateCode='+encodeURIComponent(stateCode));if(!items||country()?.value!==cn||region()?.value!==rn)return;const selected=s.value;apply(s,items,'Choose city or town',selected);s.dataset.hcRegion=key;bindCity(s);setupArea()}function bindRegion(r){if(boundRegion.has(r))return;boundRegion.add(r);r.addEventListener('change',()=>{lastRegion='';lastCity='';const s=city();if(s){s.innerHTML='';s.add(new Option('Loading cities…',''));s.disabled=true}void syncCities(true)})}function bindCity(s){if(boundCity.has(s))return;boundCity.add(s);s.addEventListener('change',()=>{lastCity='';setupArea()})}function setupArea(){const input=area();if(!input)return;addNote(label('Area / Neighborhood'),'hc-area-note','Choose or type an area, estate, neighborhood or nearby place. Suggestions load when you use this field.');let dl=document.getElementById('hc-area-suggestions-v11');if(!dl){dl=document.createElement('datalist');dl.id='hc-area-suggestions-v11';document.body.appendChild(dl)}input.setAttribute('list',dl.id);input.setAttribute('autocomplete','address-level3');if(boundArea.has(input))return;boundArea.add(input);const query=()=>{clearTimeout(areaTimer);areaTimer=setTimeout(async()=>{if(document.activeElement!==input&&input.value.trim()==='')return;const cn=country()?.value||'',ct=city()?.value||'',q=input.value.trim(),iso=countryCode.get(cn)||'';if(!iso||!ct||ct===OTHER)return;const key=iso+'|'+ct+'|'+q.toLowerCase();if(key===lastCity&&q==='')return;lastCity=key;const items=await load('/_hc/areas?iso2='+encodeURIComponent(iso)+'&city='+encodeURIComponent(ct)+'&q='+encodeURIComponent(q));if(!items||!input.isConnected)return;dl.innerHTML='';for(const name of clean(items).slice(0,30))dl.appendChild(new Option(name))},qDelay(input.value))};input.addEventListener('focus',query,{passive:true});input.addEventListener('input',query,{passive:true})}function qDelay(v){return String(v||'').trim().length>=2?300:120}function enhance(force=false){ensureCountries();const r=region(),s=city();if(r)bindRegion(r);if(s)bindCity(s);setupArea();const c=country();if(c?.value&&c.value!==OTHER)void syncRegions(force);if(c?.value&&r?.value&&r.value!==OTHER)void syncCities(force)}function schedule(force=false){if(scheduled)return;scheduled=true;requestAnimationFrame(()=>{scheduled=false;enhance(force)})}new MutationObserver(()=>schedule(false)).observe(document.documentElement,{childList:true,subtree:true});addEventListener('DOMContentLoaded',()=>schedule(true),{once:true});schedule(true)})();